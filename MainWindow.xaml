<Window x:Class="Find_File_Path.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Find_File_Path"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="400">
    <StackPanel Margin="10 10 10 10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                
            </Grid.RowDefinitions>

            <TextBox Grid.Column="0" Grid.Row="0" x:Name="txt1"/>
            <TextBox Grid.Column="0" Grid.Row="1" x:Name="txt2"/>
            
            <Button Grid.Column="1" Grid.Row="0" x:Name="btn1" Click="btn1_Click" Content=" ... "/>
            <Button Grid.Column="1" Grid.Row="1" x:Name="btn2" Click="btn2_Click" Content=" ... "/>

        </Grid>
    </StackPanel>
</Window>
